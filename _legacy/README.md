# Código legacy de ARROBA

Este código procede del repo anterior (React 19 + craco / FastAPI legacy).
**NO se reutiliza en producción.** Se conserva como referencia para:
- consulta puntual de patrones que sí funcionaban,
- migración futura de datos si se decide,
- comparación de decisiones de diseño.

La base de producción nueva vive en:
- `/app/frontend` (Next.js 14 — pendiente de scaffolding en E0.2)
- `/app/backend` (FastAPI monolito modular — pendiente de scaffolding en E0.3)

El intake de diseño vive en `/app/_design_intake/` y es referencia perpetua.
